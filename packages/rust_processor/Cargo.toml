[package]
name = "moodle-ai-processor"
version = "0.2.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
tokio = { version = "1.0", features = ["full"] } # Use full feature set for signals, etc.
async-openai = "0.29.1"
log = "0.4" # Logging facade
env_logger = "0.11" # Logging implementation